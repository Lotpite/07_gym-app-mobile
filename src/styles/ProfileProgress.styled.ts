import styled from "styled-components";
import { Col, Row } from "./Other.styled";

export const ProgressWrapper = styled(Col)`
  width: 80vw;
  gap: 10px;
`;

export const LevelProgress = styled(Col)``;

export const LevelPercent = styled.div`
  display: flex;
  flex-direction: row;
  justify-content: center;
  align-items: center;
  gap: 10px;
`;

export const DailyProgress = styled(Row)``;
